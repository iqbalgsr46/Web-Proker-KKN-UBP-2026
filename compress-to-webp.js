const sharp = require("sharp"); const path = require("path"); const dir = "public/images"; const files = ["coloring_recycle_bin.png", "coloring_plant_tree.png", "coloring_clean_river.png", "coloring_happy_earth.png", "coloring_sorting_trash.png", "coloring_bicycle_park.png"]; files.forEach(file => { sharp(path.join(dir, file)).webp({ quality: 50, effort: 6 }).toFile(path.join(dir, file.replace(".png", ".webp"))).then(() => console.log(`Converted ${file}`)); });
