const fs = require("fs"); const path = require("path"); const dir = "public/images"; const files = fs.readdirSync(dir); files.forEach(file => { if(file.endsWith("_small.webp")) { const orig = file.replace("_small", ""); fs.unlinkSync(path.join(dir, orig)); fs.renameSync(path.join(dir, file), path.join(dir, orig)); } else if(file.endsWith("_opt.webp")) { fs.unlinkSync(path.join(dir, file)); } }); console.log("Replaced successfully!");
